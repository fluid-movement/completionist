# Completionist

This is a simple todo list application built in Go. It allows you to add, view, and delete tasks from your todo list.

# Installation
Install the Completionist CLI tool using the following command:
```shell
go install github.com/fluid-movement/completionist@latest
```

Make sure GOBIN is in your path, see https://go.dev/doc/tutorial/compile-install